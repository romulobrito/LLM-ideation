#!/usr/bin/env python3
"""Compat: baseline TF-IDF (pacote embedding_models_eval)."""

import sys

from embedding_models_eval.tfidf_baseline_runner import main

if __name__ == "__main__":
    sys.exit(main())
